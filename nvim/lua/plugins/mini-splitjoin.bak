return {
  "echasnovski/mini.splitjoin",
  version = false,
  event = "BufEnter",
  config = function()
    require("mini.splitjoin").setup({
      mappings = {
        toggle = "<C-t>", -- Default: gS
        split = "",
        join = "",
      },
    })
  end,
}
